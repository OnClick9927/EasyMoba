---@class ModeServer
local M = class("ModeServer")
function M:SendBattleFrame(roomid, roleid, frame, op) end

function M:Dispose() end

---@param data SPBattleFrame
function M:SendFrameToBattle(frame)
    Battle.OnBattelFrame(frame)
end

return M
